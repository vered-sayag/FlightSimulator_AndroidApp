# FlightSimulator_AndroidApp
An Android application that communicates with the FlightSimulator application

Using the app you can move the steering wheel on the plane using the view that contains joystick
